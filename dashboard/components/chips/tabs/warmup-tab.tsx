/**
 * Warmup Tab - Sprint 45
 *
 * Wrapper component for lazy loading WarmupTabContent.
 */

import { WarmupTabContent } from '../warmup-tab-content'

export default function WarmupTab() {
  return <WarmupTabContent />
}
